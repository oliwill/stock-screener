# Tushare Token (可选，主要用于数据验证)
# 获取方式：访问 https://tushare.pro 注册并获取 Token
# 本项目主要使用 AkShare（免费无需注册），Tushare 作为备用数据源
TUSHARE_TOKEN=your_tushare_token_here

# 前端 API 地址（开发环境）
NEXT_PUBLIC_API_URL=http://localhost:8000
